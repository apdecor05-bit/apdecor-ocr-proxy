const express=require('express');const app=express();app.use(express.json({limit:'20mb'}));app.use((q,r,n)=>{r.header('Access-Control-Allow-Origin','*');r.header('Access-Control-Allow-Headers','Content-Type');r.header('Access-Control-Allow-Methods','POST,OPTIONS');if(q.method==='OPTIONS')return r.sendStatus(200);n();});app.post('/ocr',async(q,r)=>{try{const{image,mediaType}=q.body;const s=await fetch('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'Content-Type':'application/json','x-api-key':process.env.ANTHROPIC_API_KEY,'anthropic-version':'2023-06-01'},body:JSON.stringify({model:'claude-haiku-4-5-20251001',max_tokens:400,messages:[{role:'user',content:[{type:'image',source:{type:'base64',media_type:mediaType,data:image}},{type:'text',text:'Aadhaar card. Return ONLY JSON: {"name":"","aadhaar":"","dob":"","address":""}'}]}]})});const d=await s.json();const t=d.content?.[0]?.text||'{}';r.json(JSON.parse(t.replace(/```json|```/g,'').trim()));}catch(e){r.status(500).json({error:e.message});}});app.get('/',(q,r)=>r.send('OK'));app.listen(process.env.PORT||3000);
